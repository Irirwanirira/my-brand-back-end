# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Coverage

on:
  push:
    branches:
      'dev'
  pull_request:
    branches:
      'dev'

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: 20
      - name: Install Dependencies
        run: npm ci
      - name: Run tests
        env:
          MONGODB_URL: mongodb+srv://irijoseph41:vKcd0zyCyLdKdUNK@development-cluster0.u2vivmj.mongodb.net/my-brand
          PORT: 3000
          ACCESS_TOKEN_SECRET: 6ba2155ae5cd57f5d74e446799918
          REFRESH_TOKEN_SECRET: fdrfsiodjcfnvjfdnsjekmckamrf
        run: npm test
      - name: Upload coverage
        uses: npm install -g coveralls && coveralls < coverage/lcov.info


